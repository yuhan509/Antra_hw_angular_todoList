import { SummaryTitlePipe } from './summary-title.pipe';

describe('SummaryTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
